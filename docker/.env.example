# Copy this file to .env and change the values before running in shared environments.
MONGO_PORT=27017
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=change_me_in_env
MONGO_INITDB_DATABASE=myappdb
